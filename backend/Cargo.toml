[package]
name = "backend"
version = "0.1.0"

[build-dependencies]
tauri-build = { version = "2.0.3", features = [] }

[dependencies]
serde = "1"
serde_json = "1"
tauri = { version = "2", features = [] }
